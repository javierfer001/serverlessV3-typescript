Install
    - npm i -f

DataSource
    - Data source is set in Sqlite3 :memory: database
    - Data source can be changed in `src/App/dataSource.ts`

Run locally
    - npm start

Run Unit Test
    - npm test
